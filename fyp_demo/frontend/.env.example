# Dev: leave empty — Vite proxy handles /api and socket uses localhost:5000

# Production (set in Vercel/Netlify/Render static site env before build):
# VITE_API_URL=https://your-backend.onrender.com
# VITE_SOCKET_URL=https://your-backend.onrender.com
